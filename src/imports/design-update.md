Improve the hero and header design to feel more minimal and premium.

The current header feels too heavy and the grey background breaks the visual consistency.

Header improvements:

1. Remove the grey header background.

Make the header transparent so the hero background is visible behind it.

Use a subtle backdrop blur when scrolling instead of a solid color.

2. Typography

Reduce font weight across the header.

Navigation links should use:
font-weight 400

Logo should use:
font-weight 500

Avoid bold text.

3. Spacing

Increase spacing between navigation links to around 32–40px.

The header should feel lighter and more breathable.

4. Hover behavior

Navigation links should have subtle hover:

default: opacity 0.75
hover: opacity 1

No color change needed.

Hero background improvements:

Remove the grey overlay behind the text.

Use a deep black background (#050505) instead.

Keep the colored gradient glows but make them more subtle and blurred.

The grid should remain but be extremely subtle with very low opacity.

Overall goal:

The hero should feel closer to the design style of Linear, Vercel or Ashby:

* minimal
* dark
* typography-driven
* lots of empty space
* subtle lighting effects
